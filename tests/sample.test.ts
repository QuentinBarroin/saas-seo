import { describe, it, expect } from 'vitest';

/**
 * Sample test pour amorcer la pipeline Vitest.
 * À retirer dès qu'on a un vrai test (Sprint 1, S1-11).
 */
describe('scaffold sanity', () => {
  it('runs vitest', () => {
    expect(1 + 1).toBe(2);
  });
});
